{% extends "base.html" %}
{% block content %}
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">{{ pasta.name|e }}</h1>
            <p class="card-text">{{ pasta.contents|e }}</p>
        </div>
        <div class="card-footer">{{ pasta.created_at|format_time }}</div>
    </div>
{% endblock content %}
