{% extends "base.html" %}
{% block content %}
    <ul class="list-group">
        {% for pasta in pastas %}
            <a href="#"
               class="list-group-item list-group-item-action d-flex justify-content-between">
                <div>{{ pasta[0] }}</div>
                <div>{{ pasta[1] }}</div>
            </a>
        {% endfor %}
    </ul>
{% endblock content %}
